// Generated by CIRCT firtool-1.135.0

// Include register initializers in init blocks unless synthesis is set
`ifndef RANDOMIZE
  `ifdef RANDOMIZE_REG_INIT
    `define RANDOMIZE
  `endif // RANDOMIZE_REG_INIT
`endif // not def RANDOMIZE
`ifndef SYNTHESIS
  `ifndef ENABLE_INITIAL_REG_
    `define ENABLE_INITIAL_REG_
  `endif // not def ENABLE_INITIAL_REG_
`endif // not def SYNTHESIS

// Standard header to adapt well known macros for register randomization.

// RANDOM may be set to an expression that produces a 32-bit random unsigned value.
`ifndef RANDOM
  `define RANDOM $random
`endif // not def RANDOM

// Users can define INIT_RANDOM as general code that gets injected into the
// initializer block for modules with registers.
`ifndef INIT_RANDOM
  `define INIT_RANDOM
`endif // not def INIT_RANDOM

// If using random initialization, you can also define RANDOMIZE_DELAY to
// customize the delay used, otherwise 0.002 is used.
`ifndef RANDOMIZE_DELAY
  `define RANDOMIZE_DELAY 0.002
`endif // not def RANDOMIZE_DELAY

// Define INIT_RANDOM_PROLOG_ for use in our modules below.
`ifndef INIT_RANDOM_PROLOG_
  `ifdef RANDOMIZE
    `ifdef VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM
    `else  // VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM #`RANDOMIZE_DELAY begin end
    `endif // VERILATOR
  `else  // RANDOMIZE
    `define INIT_RANDOM_PROLOG_
  `endif // RANDOMIZE
`endif // not def INIT_RANDOM_PROLOG_
module Queue1_TLBundleA_2(
  input         clock,
  input         reset,
  output        io_enq_ready,
  input         io_enq_valid,
  input  [3:0]  io_enq_bits_opcode,
  input  [2:0]  io_enq_bits_param,
  input  [1:0]  io_enq_bits_size,
  input  [16:0] io_enq_bits_source,
  input  [30:0] io_enq_bits_address,
  input         io_enq_bits_user_amba_prot_bufferable,
  input         io_enq_bits_user_amba_prot_modifiable,
  input         io_enq_bits_user_amba_prot_readalloc,
  input         io_enq_bits_user_amba_prot_writealloc,
  input         io_enq_bits_user_amba_prot_privileged,
  input         io_enq_bits_user_amba_prot_secure,
  input         io_enq_bits_user_amba_prot_fetch,
  input  [3:0]  io_enq_bits_mask,
  input  [31:0] io_enq_bits_data,
  input         io_enq_bits_corrupt,
  input         io_deq_ready,
  output        io_deq_valid,
  output [3:0]  io_deq_bits_opcode,
  output [1:0]  io_deq_bits_size,
  output [16:0] io_deq_bits_source,
  output [30:0] io_deq_bits_address,
  output [31:0] io_deq_bits_data
);

  reg  [100:0] ram;
  reg          maybe_full;
  wire         io_deq_valid_0 = io_enq_valid | maybe_full;
  wire         do_enq = ~(~maybe_full & io_deq_ready) & ~maybe_full & io_enq_valid;
  always @(posedge clock) begin
    if (reset)
      maybe_full <= 1'h0;
    else if (do_enq != (maybe_full & io_deq_ready & io_deq_valid_0))
      maybe_full <= do_enq;
    if (do_enq)
      ram <=
        {io_enq_bits_opcode,
         io_enq_bits_param,
         io_enq_bits_size,
         io_enq_bits_source,
         io_enq_bits_address,
         io_enq_bits_user_amba_prot_bufferable,
         io_enq_bits_user_amba_prot_modifiable,
         io_enq_bits_user_amba_prot_readalloc,
         io_enq_bits_user_amba_prot_writealloc,
         io_enq_bits_user_amba_prot_privileged,
         io_enq_bits_user_amba_prot_secure,
         io_enq_bits_user_amba_prot_fetch,
         io_enq_bits_mask,
         io_enq_bits_data,
         io_enq_bits_corrupt};
  end // always @(posedge)
  `ifdef ENABLE_INITIAL_REG_
    `ifdef FIRRTL_BEFORE_INITIAL
      `FIRRTL_BEFORE_INITIAL
    `endif // FIRRTL_BEFORE_INITIAL
    logic [31:0] _RANDOM[0:3];
    initial begin
      `ifdef INIT_RANDOM_PROLOG_
        `INIT_RANDOM_PROLOG_
      `endif // INIT_RANDOM_PROLOG_
      `ifdef RANDOMIZE_REG_INIT
        for (logic [2:0] i = 3'h0; i < 3'h4; i += 3'h1) begin
          _RANDOM[i[1:0]] = `RANDOM;
        end
        maybe_full = _RANDOM[2'h0][0];
        ram = {_RANDOM[2'h0][31:1], _RANDOM[2'h1], _RANDOM[2'h2], _RANDOM[2'h3][5:0]};
      `endif // RANDOMIZE_REG_INIT
    end // initial
    `ifdef FIRRTL_AFTER_INITIAL
      `FIRRTL_AFTER_INITIAL
    `endif // FIRRTL_AFTER_INITIAL
  `endif // ENABLE_INITIAL_REG_
  assign io_enq_ready = ~maybe_full;
  assign io_deq_valid = io_deq_valid_0;
  assign io_deq_bits_opcode = maybe_full ? ram[100:97] : io_enq_bits_opcode;
  assign io_deq_bits_size = maybe_full ? ram[93:92] : io_enq_bits_size;
  assign io_deq_bits_source = maybe_full ? ram[91:75] : io_enq_bits_source;
  assign io_deq_bits_address = maybe_full ? ram[74:44] : io_enq_bits_address;
  assign io_deq_bits_data = maybe_full ? ram[32:1] : io_enq_bits_data;
endmodule


// Generated by CIRCT firtool-1.135.0

// Include register initializers in init blocks unless synthesis is set
`ifndef RANDOMIZE
  `ifdef RANDOMIZE_REG_INIT
    `define RANDOMIZE
  `endif // RANDOMIZE_REG_INIT
`endif // not def RANDOMIZE
`ifndef SYNTHESIS
  `ifndef ENABLE_INITIAL_REG_
    `define ENABLE_INITIAL_REG_
  `endif // not def ENABLE_INITIAL_REG_
`endif // not def SYNTHESIS

// Standard header to adapt well known macros for register randomization.

// RANDOM may be set to an expression that produces a 32-bit random unsigned value.
`ifndef RANDOM
  `define RANDOM $random
`endif // not def RANDOM

// Users can define INIT_RANDOM as general code that gets injected into the
// initializer block for modules with registers.
`ifndef INIT_RANDOM
  `define INIT_RANDOM
`endif // not def INIT_RANDOM

// If using random initialization, you can also define RANDOMIZE_DELAY to
// customize the delay used, otherwise 0.002 is used.
`ifndef RANDOMIZE_DELAY
  `define RANDOMIZE_DELAY 0.002
`endif // not def RANDOMIZE_DELAY

// Define INIT_RANDOM_PROLOG_ for use in our modules below.
`ifndef INIT_RANDOM_PROLOG_
  `ifdef RANDOMIZE
    `ifdef VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM
    `else  // VERILATOR
      `define INIT_RANDOM_PROLOG_ `INIT_RANDOM #`RANDOMIZE_DELAY begin end
    `endif // VERILATOR
  `else  // RANDOMIZE
    `define INIT_RANDOM_PROLOG_
  `endif // RANDOMIZE
`endif // not def INIT_RANDOM_PROLOG_
module Trigger2_Tdata3Module(
  input         clock,
  input         reset,
  input         w_wen,
  input  [63:0] w_wdata,
  output [63:0] rdata
);

  reg [1:0]  reg_SSELECT;
  reg [31:0] reg_SVALUE;
  reg [3:0]  reg_SBYTEMASK;
  reg [2:0]  reg_MHSELECT;
  reg [12:0] reg_MHVALUE;
  always @(posedge clock or posedge reset) begin
    if (reset) begin
      reg_SSELECT <= 2'h0;
      reg_SVALUE <= 32'h0;
      reg_SBYTEMASK <= 4'h0;
      reg_MHSELECT <= 3'h0;
      reg_MHVALUE <= 13'h0;
    end
    else begin
      if (w_wen & (w_wdata[1:0] == 2'h0 | w_wdata[1:0] == 2'h1 | w_wdata[1:0] == 2'h2))
        reg_SSELECT <= w_wdata[1:0];
      if (w_wen) begin
        reg_SVALUE <= w_wdata[33:2];
        reg_SBYTEMASK <= w_wdata[39:36];
        reg_MHVALUE <= w_wdata[63:51];
      end
      if (w_wen
          & (w_wdata[50:48] == 3'h0 | w_wdata[50:48] == 3'h1 | w_wdata[50:48] == 3'h2
             | w_wdata[50:48] == 3'h4 | w_wdata[50:48] == 3'h5 | w_wdata[50:48] == 3'h6))
        reg_MHSELECT <= w_wdata[50:48];
    end
  end // always @(posedge, posedge)
  `ifdef ENABLE_INITIAL_REG_
    `ifdef FIRRTL_BEFORE_INITIAL
      `FIRRTL_BEFORE_INITIAL
    `endif // FIRRTL_BEFORE_INITIAL
    logic [31:0] _RANDOM[0:1];
    initial begin
      `ifdef INIT_RANDOM_PROLOG_
        `INIT_RANDOM_PROLOG_
      `endif // INIT_RANDOM_PROLOG_
      `ifdef RANDOMIZE_REG_INIT
        for (logic [1:0] i = 2'h0; i < 2'h2; i += 2'h1) begin
          _RANDOM[i[0]] = `RANDOM;
        end
        reg_SSELECT = _RANDOM[1'h0][1:0];
        reg_SVALUE = {_RANDOM[1'h0][31:2], _RANDOM[1'h1][1:0]};
        reg_SBYTEMASK = _RANDOM[1'h1][5:2];
        reg_MHSELECT = _RANDOM[1'h1][8:6];
        reg_MHVALUE = _RANDOM[1'h1][21:9];
      `endif // RANDOMIZE_REG_INIT
      if (reset) begin
        reg_SSELECT = 2'h0;
        reg_SVALUE = 32'h0;
        reg_SBYTEMASK = 4'h0;
        reg_MHSELECT = 3'h0;
        reg_MHVALUE = 13'h0;
      end
    end // initial
    `ifdef FIRRTL_AFTER_INITIAL
      `FIRRTL_AFTER_INITIAL
    `endif // FIRRTL_AFTER_INITIAL
  `endif // ENABLE_INITIAL_REG_
  assign rdata =
    {reg_MHVALUE, reg_MHSELECT, 8'h0, reg_SBYTEMASK, 2'h0, reg_SVALUE, reg_SSELECT};
endmodule

